---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/src/stores/uiStore.ts
  - client/src/stores/index.ts
  - client/src/__tests__/setup.ts
  - client/src/__tests__/stores/uiStore.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Developer can run npm run dev without errors"
    - "Zustand store is importable from any component"
    - "UI state persists across page refresh"
    - "Test suite runs with npm test"
  artifacts:
    - path: "client/package.json"
      provides: "Dependencies installed"
      contains: "zustand"
    - path: "client/src/stores/uiStore.ts"
      provides: "UI state management"
      exports: ["useUIStore"]
      min_lines: 30
    - path: "client/src/__tests__/setup.ts"
      provides: "Test utilities"
      min_lines: 10
  key_links:
    - from: "client/src/stores/uiStore.ts"
      to: "localStorage"
      via: "persist middleware"
      pattern: "persist\\("
---

<objective>
Install missing dependencies (Zustand, TanStack Table, testing-library) and establish client-side state management with testing infrastructure.

Purpose: Foundation for UI state management and test-driven development
Output: Working Zustand store, test setup, dependencies installed
</objective>

<execution_context>
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Current package.json state
@client/package.json

# Existing patterns to follow
@client/src/lib/queryClient.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies</name>
  <files>client/package.json</files>
  <behavior>
    - Test 1: zustand@5.0.11 is in dependencies
    - Test 2: @tanstack/react-table@8.21.3 is in dependencies
    - Test 3: @testing-library/react is in devDependencies
  </behavior>
  <action>
    Install required dependencies for Phase 1:
    
    ```bash
    cd client
    npm install zustand@5.0.11 @tanstack/react-table@8.21.3
    npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event
    ```
    
    Verify installation:
    - zustand version should be 5.0.11 (exact)
    - @tanstack/react-table version should be 8.21.3 (exact)
    - Testing libraries should be latest compatible versions
    
    Do NOT install:
    - @tanstack/react-virtual (deferred to Phase 2 per RESEARCH.md)
    - Redux Toolkit (overkill per RESEARCH.md)
    - Axios (using native fetch per RESEARCH.md)
  </action>
  <verify>
    <automated>cd client && npm list zustand @tanstack/react-table @testing-library/react --depth=0</automated>
  </verify>
  <done>
    - package.json contains zustand@5.0.11
    - package.json contains @tanstack/react-table@8.21.3
    - package.json contains @testing-library/react in devDependencies
    - npm install completes without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zustand UI store</name>
  <files>client/src/stores/uiStore.ts, client/src/stores/index.ts, client/src/__tests__/stores/uiStore.test.ts</files>
  <behavior>
    - Test 1: useUIStore hook is callable and returns initial state
    - Test 2: toggleSidebar changes sidebarOpen from true to false
    - Test 3: setTheme changes theme value
    - Test 4: State persists to localStorage under key 'coldbot-ui-storage'
  </behavior>
  <action>
    Create Zustand store for UI state management following RESEARCH.md pattern:
    
    1. Create `client/src/stores/uiStore.ts`:
       - Import: create from zustand, persist and createJSONStorage from zustand/middleware
       - Define UIState interface with:
         * sidebarOpen: boolean (default: true)
         * theme: 'light' | 'dark' | 'system' (default: 'system')
         * activeModal: string | null (default: null)
         * toggleSidebar: () => void
         * setSidebarOpen: (open: boolean) => void
         * setTheme: (theme: 'light' | 'dark' | 'system') => void
         * openModal: (id: string) => void
         * closeModal: () => void
       - Use persist() middleware with:
         * name: 'coldbot-ui-storage'
         * storage: createJSONStorage(() => localStorage)
         * partialize: only persist sidebarOpen and theme
       - Export useUIStore hook
    
    2. Create `client/src/stores/index.ts`:
       - Re-export useUIStore from './uiStore'
    
    3. Create test file `client/src/__tests__/stores/uiStore.test.ts`:
       - Test initial state (sidebarOpen: true, theme: 'system')
       - Test toggleSidebar toggles boolean
       - Test setTheme changes theme
       - Test openModal/closeModal changes activeModal
    
    Follow pattern from RESEARCH.md lines 308-354.
    Do NOT store server state (contacts, campaigns) - use TanStack Query for that.
  </action>
  <verify>
    <automated>cd client && npm test -- --run src/__tests__/stores/uiStore.test.ts</automated>
  </verify>
  <done>
    - useUIStore hook is importable from '@/stores'
    - Store persists to localStorage
    - All 4 tests pass
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test setup file</name>
  <files>client/src/__tests__/setup.ts</files>
  <action>
    Create test setup file for testing-library configuration:
    
    Create `client/src/__tests__/setup.ts`:
    - Import @testing-library/jest-dom (extends Jest matchers)
    - Add any global test utilities needed
    - Configure testing-library cleanup if needed
    
    This file will be imported by Vitest config (already configured at root level).
    
    Reference: RESEARCH.md lines 489-495
  </action>
  <verify>
    <automated>cd client && npm test -- --run 2>&1 | grep -i "setup" || echo "Tests run without setup errors"</automated>
  </verify>
  <done>
    - setup.ts file exists
    - Imports @testing-library/jest-dom
    - Test suite runs without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `cd client && npm run dev` - should start without errors
2. Run `cd client && npm test` - should find and run tests
3. Import useUIStore in a test file - should be accessible
4. Check localStorage after using store - should persist state
</verification>

<success_criteria>
- [ ] Dependencies installed: zustand@5.0.11, @tanstack/react-table@8.21.3, testing-library
- [ ] Zustand store created with persist middleware
- [ ] Test setup file created
- [ ] All tests pass
- [ ] npm run dev works without errors
- [ ] Store state persists to localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
