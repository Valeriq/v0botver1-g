---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - client/src/components/layout/Header.tsx
  - client/src/components/layout/AppLayout.tsx
  - client/src/components/layout/Sidebar.tsx
  - client/src/__tests__/components/layout/AppLayout.test.tsx
  - client/src/__tests__/components/layout/Sidebar.test.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can see Header with search and user menu"
    - "User can toggle Sidebar visibility"
    - "Sidebar state persists across page refresh"
    - "Layout is responsive on mobile devices"
  artifacts:
    - path: "client/src/components/layout/Header.tsx"
      provides: "Top navigation bar"
      exports: ["Header"]
      min_lines: 40
    - path: "client/src/components/layout/AppLayout.tsx"
      provides: "Main layout wrapper"
      contains: "Header"
    - path: "client/src/components/layout/Sidebar.tsx"
      provides: "Responsive sidebar navigation"
      contains: "useUIStore"
  key_links:
    - from: "client/src/components/layout/Header.tsx"
      to: "useUIStore"
      via: "toggleSidebar"
      pattern: "useUIStore"
    - from: "client/src/components/layout/Sidebar.tsx"
      to: "useUIStore"
      via: "sidebarOpen state"
      pattern: "useUIStore"
    - from: "client/src/components/layout/AppLayout.tsx"
      to: "useUIStore"
      via: "sidebarOpen conditional rendering"
      pattern: "useUIStore"
---

<objective>
Add Header component with user menu and search, enhance AppLayout to include Header, and make Sidebar responsive using Zustand state management.

Purpose: Complete the layout structure with proper navigation and responsive design
Output: Functional Header, responsive Sidebar, enhanced AppLayout
</objective>

<execution_context>
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Current layout components
@client/src/components/layout/AppLayout.tsx
@client/src/components/layout/Sidebar.tsx

# Zustand store (from Plan 01-01)
@client/src/stores/uiStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Header component</name>
  <files>client/src/components/layout/Header.tsx</files>
  <behavior>
    - Test 1: Header renders with menu button
    - Test 2: Header renders with search input
    - Test 3: Clicking menu button calls toggleSidebar
    - Test 4: Header displays user avatar
  </behavior>
  <action>
    Create Header component with search and user menu:
    
    Create `client/src/components/layout/Header.tsx`:
    - Import: useAuth from '@/contexts/AuthContext', useUIStore from '@/stores'
    - Import icons: Menu, Search, Bell from 'lucide-react'
    - Import UI components: Button, Input, Avatar from '@/components/ui'
    - Structure:
      * Fixed height (h-16)
      * Border bottom (border-b border-border/50)
      * Background (bg-card)
      * Flex layout with items-center, justify-between
      * Left section: Menu button + Search input
      * Right section: Notifications + User menu
    - Menu button:
      * onClick: toggleSidebar from useUIStore
      * Icon: Menu
      * Variant: ghost, size: icon
    - Search input:
      * Placeholder: "Search contacts, campaigns..."
      * Icon: Search (positioned absolute left)
      * Width: w-96 (or responsive)
      * Class: pl-10 (padding for icon)
    - Notifications button:
      * Icon: Bell
      * Variant: ghost, size: icon
    - User menu:
      * Avatar from useAuth().user
      * Fallback: user initials
    
    Reference: RESEARCH.md lines 732-762
    Keep search as placeholder (no functionality yet).
  </action>
  <verify>
    <automated>cd client && npm test -- --run src/__tests__/components/layout/Header.test.tsx 2>&1 || echo "Header component created (tests in next task)"</automated>
  </verify>
  <done>
    - Header component exists
    - Menu button calls toggleSidebar
    - Search input is visible
    - User avatar is displayed
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance AppLayout with Header</name>
  <files>client/src/components/layout/AppLayout.tsx, client/src/__tests__/components/layout/AppLayout.test.tsx</files>
  <behavior>
    - Test 1: AppLayout renders Header component
    - Test 2: AppLayout renders children
    - Test 3: Main content has correct margin based on sidebarOpen
    - Test 4: Layout structure is flex column
  </behavior>
  <action>
    Enhance AppLayout to include Header and use Zustand state:
    
    Modify `client/src/components/layout/AppLayout.tsx`:
    - Import: Header from './Header', useUIStore from '@/stores'
    - Import: cn from '@/lib/utils' (for conditional classes)
    - Get sidebarOpen from useUIStore()
    - Structure:
      * Outer div: min-h-screen, bg-background, flex
      * Sidebar component (existing)
      * Inner div: flex-1, flex-col, transition-all duration-300
      * Conditional margin: sidebarOpen ? "ml-64" : "ml-0"
      * Header component (new)
      * Main: flex-1, overflow-auto, p-6
      * Children in main
    - Remove: Toaster from AppLayout (already in App.tsx)
    - Remove: pl-64 from main (use dynamic margin on parent)
    
    Create test file `client/src/__tests__/components/layout/AppLayout.test.tsx`:
    - Test Header is rendered
    - Test children are rendered
    - Test margin changes based on sidebarOpen
    - Test layout structure
    
    Reference: RESEARCH.md lines 767-791
  </action>
  <verify>
    <automated>cd client && npm test -- --run src/__tests__/components/layout/AppLayout.test.tsx</automated>
  </verify>
  <done>
    - Header is rendered in AppLayout
    - Sidebar margin is dynamic based on state
    - Layout uses flex column structure
    - All 4 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make Sidebar responsive with Zustand</name>
  <files>client/src/components/layout/Sidebar.tsx, client/src/__tests__/components/layout/Sidebar.test.tsx</files>
  <behavior>
    - Test 1: Sidebar is visible when sidebarOpen is true
    - Test 2: Sidebar is hidden when sidebarOpen is false
    - Test 3: Sidebar uses useUIStore for state
    - Test 4: Navigation links are rendered
  </behavior>
  <action>
    Make Sidebar responsive using Zustand state:
    
    Modify `client/src/components/layout/Sidebar.tsx`:
    - Import: useUIStore from '@/stores'
    - Import: cn from '@/lib/utils' (if not already)
    - Get sidebarOpen from useUIStore()
    - Update outer div classes:
      * Add: transition-transform duration-300
      * Add conditional: !sidebarOpen && "-translate-x-full"
      * Keep: fixed, left-0, top-0, z-50, h-screen, w-64, border-r, bg-card
    - Keep existing navigation structure
    - Keep existing user info section
    - Keep existing logout functionality
    
    Create test file `client/src/__tests__/components/layout/Sidebar.test.tsx`:
    - Test Sidebar renders navigation links
    - Test Sidebar visibility based on sidebarOpen
    - Test useUIStore is called
    - Test logout button exists
    
    Reference: RESEARCH.md lines 796-811
    Do NOT change navigation items or user section.
  </action>
  <verify>
    <automated>cd client && npm test -- --run src/__tests__/components/layout/Sidebar.test.tsx</automated>
  </verify>
  <done>
    - Sidebar uses useUIStore for state
    - Sidebar animates in/out with transition
    - Sidebar is hidden when sidebarOpen is false
    - All 4 tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run dev` and navigate to app
2. Verify Header is visible at top
3. Click menu button - Sidebar should toggle
4. Refresh page - Sidebar state should persist
5. Resize browser - Layout should be responsive
</verification>

<success_criteria>
- [ ] Header component created with search and user menu
- [ ] AppLayout enhanced to include Header
- [ ] Sidebar made responsive with Zustand state
- [ ] Sidebar state persists across page refresh
- [ ] All tests pass
- [ ] Layout is responsive on different screen sizes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
