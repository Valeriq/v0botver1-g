---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/apiClient.ts
  - client/src/lib/queryKeys.ts
  - client/src/lib/queryClient.ts
  - client/src/__tests__/lib/apiClient.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "API client can make GET requests to backend"
    - "API client can make POST requests with JSON body"
    - "API errors are caught and thrown as APIError"
    - "Query keys are type-safe and hierarchical"
  artifacts:
    - path: "client/src/lib/apiClient.ts"
      provides: "Enhanced API client with error handling"
      exports: ["apiRequest", "APIError", "api"]
      min_lines: 40
    - path: "client/src/lib/queryKeys.ts"
      provides: "Type-safe query key factory"
      exports: ["queryKeys"]
      min_lines: 30
    - path: "client/src/lib/queryClient.ts"
      provides: "TanStack Query configuration"
      contains: "QueryClient"
  key_links:
    - from: "client/src/lib/apiClient.ts"
      to: "fetch API"
      via: "native fetch"
      pattern: "fetch\\("
    - from: "client/src/lib/queryKeys.ts"
      to: "TanStack Query"
      via: "queryKey arrays"
      pattern: "\\['\\w+'\\]"
---

<objective>
Enhance API client with better error handling and create type-safe query keys factory for TanStack Query cache management.

Purpose: Standardize API calls and cache key management across the application
Output: Enhanced API client, query keys factory, base API hooks
</objective>

<execution_context>
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP i5 1135/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Current API client implementation
@client/src/lib/queryClient.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create enhanced API client</name>
  <files>client/src/lib/apiClient.ts, client/src/__tests__/lib/apiClient.test.ts</files>
  <behavior>
    - Test 1: apiRequest('GET', '/api/test') returns Response
    - Test 2: apiRequest throws APIError on 404 status
    - Test 3: APIError has status, statusText, and message properties
    - Test 4: api.get('/test') is shorthand for apiRequest('GET', '/test')
  </behavior>
  <action>
    Create enhanced API client with custom error class:
    
    1. Create `client/src/lib/apiClient.ts`:
       - Define APIError class extending Error:
         * constructor(status: number, statusText: string, message: string)
         * Store status, statusText as public properties
         * Set name = 'APIError'
       - Export apiRequest function (move from queryClient.ts):
         * Keep existing implementation (credentials: "include")
         * Throw APIError instead of generic Error
         * Keep headers and body logic
       - Export api convenience object:
         * api.get(url) → apiRequest('GET', url)
         * api.post(url, data) → apiRequest('POST', url, data)
         * api.put(url, data) → apiRequest('PUT', url, data)
         * api.delete(url) → apiRequest('DELETE', url)
    
    2. Create test file `client/src/__tests__/lib/apiClient.test.ts`:
       - Mock fetch globally
       - Test successful GET request
       - Test error handling (404, 500)
       - Test APIError properties
       - Test api convenience methods
    
    Reference: RESEARCH.md lines 653-694
    Keep existing cookie-based auth (no JWT interceptors needed).
  </action>
  <verify>
    <automated>cd client && npm test -- --run src/__tests__/lib/apiClient.test.ts</automated>
  </verify>
  <done>
    - APIError class exists with status, statusText, message
    - apiRequest throws APIError on non-OK responses
    - api convenience methods work
    - All 4 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create query keys factory</name>
  <files>client/src/lib/queryKeys.ts</files>
  <behavior>
    - Test 1: queryKeys.contacts.all returns ['contacts']
    - Test 2: queryKeys.contacts.list() returns ['contacts', 'list']
    - Test 3: queryKeys.contacts.detail('123') returns ['contacts', 'detail', '123']
    - Test 4: All keys are readonly (as const)
  </behavior>
  <action>
    Create type-safe query keys factory for TanStack Query:
    
    Create `client/src/lib/queryKeys.ts`:
    - Export const queryKeys object with hierarchical structure:
      * contacts: { all, list(), detail(id) }
      * campaigns: { all, list(), detail(id) }
      * leads: { all, list(), detail(id) }
      * emails: { all, list(), detail(id) }
      * gmailAccounts: { all, list(), detail(id) }
      * promptProfiles: { all, list(), detail(id) }
    - Use 'as const' for type safety
    - Follow factory pattern: all → list → detail
    
    Example structure:
    ```typescript
    export const queryKeys = {
      contacts: {
        all: ['contacts'] as const,
        list: () => [...queryKeys.contacts.all, 'list'] as const,
        detail: (id: string) => [...queryKeys.contacts.all, 'detail', id] as const,
      },
      // ... other entities
    } as const;
    ```
    
    Reference: RESEARCH.md lines 698-716
    This replaces the current pattern of using full paths as keys.
  </action>
  <verify>
    <automated>cd client && npm test -- --run 2>&1 | grep -i "queryKeys" || echo "Query keys module loads without errors"</automated>
  </verify>
  <done>
    - queryKeys object exported
    - Hierarchical structure for all entities
    - Type-safe with 'as const'
    - All keys are arrays of strings
  </done>
</task>

<task type="auto">
  <name>Task 3: Update queryClient to use new API client</name>
  <files>client/src/lib/queryClient.ts</files>
  <action>
    Update existing queryClient.ts to import from apiClient:
    
    Modify `client/src/lib/queryClient.ts`:
    - Import apiRequest from './apiClient'
    - Remove the apiRequest function definition (moved to apiClient.ts)
    - Keep throwIfResNotOk function (used by getQueryFn)
    - Keep getQueryFn function unchanged
    - Keep queryClient configuration unchanged
    
    This refactors the code to separate concerns:
    - apiClient.ts: HTTP request logic and error handling
    - queryClient.ts: TanStack Query configuration
    
    Do NOT change:
    - getQueryFn implementation (works correctly)
    - queryClient default options (correct settings)
    - Cookie-based auth (credentials: "include")
  </action>
  <verify>
    <automated>cd client && npm run build 2>&1 | grep -i "error" || echo "Build succeeds without errors"</automated>
  </verify>
  <done>
    - apiRequest imported from apiClient.ts
    - No duplicate apiRequest definition
    - queryClient configuration unchanged
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Import api from '@/lib/apiClient' - should have get, post, put, delete methods
2. Import queryKeys from '@/lib/queryKeys' - should have hierarchical structure
3. Run test suite - all tests should pass
4. Build project - should compile without errors
</verification>

<success_criteria>
- [ ] API client enhanced with APIError class
- [ ] Query keys factory created with hierarchical structure
- [ ] queryClient.ts refactored to use apiClient
- [ ] All tests pass
- [ ] TypeScript compiles without errors
- [ ] API convenience methods (api.get, api.post) work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
